/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // A chave 'theme' foi REMOVIDA para que o Tailwind use os padrões.
  plugins: [],
};
